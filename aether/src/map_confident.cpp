#include "aether/map_confident.hpp"

#include "aether/map_in_progress.hpp"

float MapConfident::probability(float x, float y, float std) const {
    // return map_.probability(x, y, std);
    return 0.0f;
}
